import 'package:flutter/material.dart';

class PersonInitialPage extends StatelessWidget {
  const PersonInitialPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
          child: Text('Person Initial Page')
      ),
    );
  }
}